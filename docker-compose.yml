version: '3.8'

services:
  api:
    platform: linux/amd64
    build:
      context: .
      dockerfile: Dockerfile
    container_name: beko_api
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://admin:1234@db:5432/bekodb
    depends_on:
      - db
    networks:
      - app-network
    restart: always

  db:
    platform: linux/amd64
    image: postgres:latest
    container_name: postgres_local
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: bekodb
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app-network

  prisma:
    platform: linux/amd64
    image: node:18
    volumes:
      - .:/app
    working_dir: /app
    environment:
      DATABASE_URL: 'postgresql://admin:1234@db:5432/bekodb'
    depends_on:
      - db
    networks:
      - app-network
    entrypoint:
      [
        'sh',
        '-c',
        'npx prisma generate --schema=prisma/postsql.prisma && npx prisma migrate dev --schema=prisma/postsql.prisma --name init',
      ]

  ngrinder-controller:
    platform: linux/amd64
    image: ngrinder/controller
    container_name: ngrinder-controller
    restart: always
    ports:
      - '9000:80'
      - '16001:16001'
      - '12000-12009:12000-12009'
    volumes:
      - ./ngrinder-controller:/opt/ngrinder-controller
    networks:
      - app-network

  ngrinder-agent:
    platform: linux/amd64
    image: ngrinder/agent
    container_name: ngrinder-agent
    restart: always
    depends_on:
      - ngrinder-controller
    networks:
      - app-network
    environment:
      - CONTROLLER_ADDR=ngrinder-controller
    volumes:
      - ./ngrinder-agent:/opt/ngrinder-agent

volumes:
  postgres_data:
  ngrinder-data:

networks:
  app-network:
    driver: bridge
